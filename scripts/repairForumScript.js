// In case of getting unclosed script tag in forum additional HTML - please uncomment this code, write proper selector and customize the code for removing tag from DOM.

// const scriptEl = document.querySelector(
//     "#announce > .box > .inbox > div"
// );

// const replaced = scriptEl.innerHTML.replace('<script>', '');
// scriptEl.innerHTML = replaced;
